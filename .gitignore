node_modules/
dist/
.env
.env.local
*.log
.DS_Store
coverage/
.vitest-cache/
/var/

# Per-user OAuth refresh token + secrets — must never be checked in.
oauth-token/
oauth-token.json
.local/

# Cross-family auditor output (codex-audit) — local per machine
.claude/audits/*
!.claude/audits/README.md
